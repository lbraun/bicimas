<p id="notice"><%= notice %></p>

<h1><%= link_to @station_status_record.station, @station_status_record.station %></h1>

<p>
  <strong>Status as of:</strong>
  <%= @station_status_record.created_at.to_s(:long) %>
  <%= link_to "<< Earlier", @station_status_record.previous_record %> |
  <%= link_to "Later >>", @station_status_record.next_record %>
</p>

<p>
  <strong>Bikes present:</strong>
  <%= @station_status_record.bikes_total %>
</p>

<p>
  <strong>Bikes available:</strong>
  <%= @station_status_record.bikes_available %>
</p>

<p>
  <strong>Anchors:</strong>
  <table class="bench">
    <tr>
      <% @station_status_record.anchor_records.each do |anchor| %>
        <td class="label"><%= anchor["number"] %></td>
      <% end %>
    </tr>
    <tr>
      <% @station_status_record.anchor_records.each do |anchor| %>
        <% if anchor["bicycle"].present? %>
          <% if anchor["incidents"].present? %>
            <td class="label label-yellow">
              <%= anchor["bicycle"] %>
              <br>
              😢
            </td>
          <% else %>
            <td class="label label-green">
              <%= anchor["bicycle"] %>
              <br>
              😀
            </td>
          <% end %>
        <% else %>
            <td></td>
        <% end %>
      <% end %>
    </tr>
  </table>
</p>

<p>
  <strong>Last seen:</strong>
  <%= @station_status_record.last_seen %>
</p>

<p>
  <strong>Online:</strong>
  <%= @station_status_record.online %>
</p>

<p>
  <strong>Ip:</strong>
  <%= @station_status_record.ip %>
</p>

<p>
  <strong>Number loans:</strong>
  <%= @station_status_record.number_loans %>
</p>
